<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>com.skyflow</groupId>
        <artifactId>skyflow</artifactId>
        <version>1.0.0</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <artifactId>skyflow-java</artifactId>
    <version>2.1.1</version>
    <groupId>com.skyflow</groupId>

    <properties>
        <maven.deploy.skip>false</maven.deploy.skip>
        <maven.compiler.source>8</maven.compiler.source>
        <maven.compiler.target>8</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <sdk.version>${project.version}</sdk.version>
    </properties>

    <dependencies>
        <dependency>
            <groupId>com.skyflow</groupId>
            <artifactId>common</artifactId>
            <version>1.0.0</version>
        </dependency>

    </dependencies>

    <build>
        <resources>
            <resource>
                <directory>src/main/resources</directory>
                <filtering>true</filtering>
            </resource>
        </resources>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
                <version>3.6.0</version>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>shade</goal>
                        </goals>
                        <configuration>
                            <artifactSet>
                                <includes>
                                    <include>com.skyflow:common</include>
                                </includes>
                            </artifactSet>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <!-- Produces an additional, comparison-only jar that merges in com.skyflow:common,
                     same pattern flowvault uses. It exists only so japicmp can compare an
                     apples-to-apples "everything a v2 consumer actually gets" surface against the
                     old 2.1.0 baseline, which was a single self-contained jar (predating the
                     common/v2 split). Without it, classes that moved into common (Credentials,
                     ErrorCode, BearerToken, Token, etc.) would show up as false-positive "removed"
                     classes, since japicmp only compares what's physically inside the two jars it's
                     given. Build-time only: it is never an artifact. -->
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
                <version>3.6.0</version>
                <executions>
                    <execution>
                        <id>shade-for-japicmp</id>
                        <phase>package</phase>
                        <goals>
                            <goal>shade</goal>
                        </goals>
                        <configuration>
                            <!-- outputFile, NOT shadedArtifactAttached. Attaching makes this a real
                                 project artifact, and Maven installs and DEPLOYS attached artifacts -
                                 a -with-common.jar would ship to jfrog and Maven Central alongside
                                 the real one. outputFile writes the file to disk for japicmp to read
                                 and attaches nothing. It lands in the repo-root api-report/build/,
                                 outside every module, so nothing packaged or published can see it. -->
                            <outputFile>${project.basedir}/../api-report/build/${project.build.finalName}-with-common.jar</outputFile>
                            <createDependencyReducedPom>false</createDependencyReducedPom>
                            <artifactSet>
                                <includes>
                                    <include>com.skyflow:common</include>
                                </includes>
                            </artifactSet>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>com.github.siom79.japicmp</groupId>
                <artifactId>japicmp-maven-plugin</artifactId>
                <version>0.26.0</version>
                <configuration>
                    <!-- No published version is pulled for this comparison. "Old" is the
                         committed contract snapshot (repo-root api-report/skyflow-java.baseline.jar) -
                         a jar built from the last code state a human deliberately approved as
                         the contract, same role as skyflow-node's committed api-report/*.api.md.
                         Regenerate it with scripts/contract-snapshot-update.sh after an
                         intentional public API change, then commit the updated jar. -->
                    <oldVersion>
                        <file>
                            <path>${project.basedir}/../api-report/skyflow-java.baseline.jar</path>
                        </file>
                    </oldVersion>
                    <newVersion>
                        <file>
                            <path>${project.basedir}/../api-report/build/${project.build.finalName}-with-common.jar</path>
                        </file>
                    </newVersion>
                    <parameter>
                        <accessModifier>protected</accessModifier>
                        <onlyModified>true</onlyModified>
                        <ignoreMissingClasses>false</ignoreMissingClasses>
                        <breakBuildOnBinaryIncompatibleModifications>true</breakBuildOnBinaryIncompatibleModifications>
                        <breakBuildOnSourceIncompatibleModifications>true</breakBuildOnSourceIncompatibleModifications>
                        <!-- An allowlist, not a blocklist: the contract is exactly what a customer
                             imports. Everything unlisted - com.skyflow.generated.*, com.skyflow.logs,
                             com.skyflow.utils.* - is internal and free to change without a baseline
                             update. Note japicmp compares signatures only, never method bodies, so
                             the controller entry below gates its method signatures, not how they
                             are implemented. Kept identical to flowvault's list, minus the packages
                             that module does not have.

                             com.skyflow.VaultClient is deliberately NOT listed. Every one of its
                             members is protected - generated-client getters, request-body builders,
                             bearer-token refresh - so it has no public API, and it only ever showed
                             up here because accessModifier=protected sweeps protected members in.
                             Its subclasses (VaultController, DetectController) are final, so no
                             customer can subclass their way to a protected member either. Please
                             do not add it back. -->
                        <includes>
                            <include>com.skyflow.Skyflow</include>
                            <include>com.skyflow.config</include>
                            <include>com.skyflow.enums</include>
                            <include>com.skyflow.errors</include>
                            <include>com.skyflow.serviceaccount.util</include>
                            <include>com.skyflow.vault.audit</include>
                            <include>com.skyflow.vault.bin</include>
                            <include>com.skyflow.vault.connection</include>
                            <include>com.skyflow.vault.controller</include>
                            <include>com.skyflow.vault.data</include>
                            <include>com.skyflow.vault.detect</include>
                            <include>com.skyflow.vault.tokens</include>
                        </includes>
                        <skipXmlReport>false</skipXmlReport>
                        <skipHtmlReport>false</skipHtmlReport>
                    </parameter>
                </configuration>
                <executions>
                    <execution>
                        <id>default-cli</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>cmp</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
        <profiles>
        <profile>
            <id>jfrog</id>
            <distributionManagement>
                <repository>
                    <id>central</id>
                    <name>prekarilabs.jfrog.io-releases</name>
                    <url>https://prekarilabs.jfrog.io/artifactory/skyflow-java</url>
                </repository>
                <snapshotRepository>
                    <id>snapshots</id>
                    <name>prekarilabs.jfrog.io-snapshots</name>
                    <url>https://prekarilabs.jfrog.io/artifactory/skyflow-java</url>
                </snapshotRepository>
            </distributionManagement>
        </profile>
        <profile>
            <id>maven-central</id>
            <distributionManagement>
                <repository>
                    <id>central</id>
                    <url>https://central.sonatype.com/api/v1/publisher/upload</url>
                </repository>
                <snapshotRepository>
                    <id>central-snapshots</id>
                    <url>https://central.sonatype.com/api/v1/publisher/upload</url>
                </snapshotRepository>
            </distributionManagement>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.sonatype.central</groupId>
                        <artifactId>central-publishing-maven-plugin</artifactId>
                        <version>0.4.0</version>
                        <extensions>true</extensions>
                        <configuration>
                            <publishingServerId>central</publishingServerId>
                            <tokenAuth>true</tokenAuth>
                            <!-- See flowvault/pom.xml: releases stage as VALIDATED in the
                                 Central Portal and need a human to click Publish. Note this
                                 changes v2's behaviour - releases no longer go live on their
                                 own once mvn deploy succeeds. -->
                            <autoPublish>false</autoPublish>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
