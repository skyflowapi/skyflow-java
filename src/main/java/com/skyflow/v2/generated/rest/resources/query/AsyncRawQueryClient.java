/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.skyflow.v2.generated.rest.resources.query;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.skyflow.v2.generated.rest.core.ApiClientApiException;
import com.skyflow.v2.generated.rest.core.ApiClientException;
import com.skyflow.v2.generated.rest.core.ApiClientHttpResponse;
import com.skyflow.v2.generated.rest.core.ClientOptions;
import com.skyflow.v2.generated.rest.core.MediaTypes;
import com.skyflow.v2.generated.rest.core.ObjectMappers;
import com.skyflow.v2.generated.rest.core.RequestOptions;
import com.skyflow.v2.generated.rest.errors.NotFoundError;
import com.skyflow.v2.generated.rest.resources.query.requests.QueryServiceExecuteQueryBody;
import com.skyflow.v2.generated.rest.types.V1GetQueryResponse;
import java.io.IOException;
import java.util.concurrent.CompletableFuture;
import okhttp3.Call;
import okhttp3.Callback;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;
import org.jetbrains.annotations.NotNull;

public class AsyncRawQueryClient {
    protected final ClientOptions clientOptions;

    public AsyncRawQueryClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    /**
     * Returns records for a valid SQL query. This endpoint &lt;ul&gt;&lt;li&gt;Can return redacted record values.&lt;/li&gt;&lt;li&gt;Supports only the &lt;code&gt;SELECT&lt;/code&gt; command.&lt;/li&gt;&lt;li&gt;Returns a maximum of 25 records. To return additional records, perform another query using the &lt;code&gt;OFFSET&lt;/code&gt; keyword.&lt;/li&gt;&lt;li&gt;Can't modify the vault or perform transactions.&lt;/li&gt;&lt;li&gt;Can't return tokens.&lt;/li&gt;&lt;li&gt;Can't return file download or render URLs.&lt;/li&gt;&lt;li&gt;Doesn't support the &lt;code&gt;WHERE&lt;/code&gt; keyword with columns using transient tokenization.&lt;/li&gt;&lt;li&gt;Doesn't support <code>?</code> conditional for columns with column-level encryption disabled.&lt;/li&gt;&lt;ul&gt;
     */
    public CompletableFuture<ApiClientHttpResponse<V1GetQueryResponse>> queryServiceExecuteQuery(String vaultId) {
        return queryServiceExecuteQuery(
                vaultId, QueryServiceExecuteQueryBody.builder().build());
    }

    /**
     * Returns records for a valid SQL query. This endpoint &lt;ul&gt;&lt;li&gt;Can return redacted record values.&lt;/li&gt;&lt;li&gt;Supports only the &lt;code&gt;SELECT&lt;/code&gt; command.&lt;/li&gt;&lt;li&gt;Returns a maximum of 25 records. To return additional records, perform another query using the &lt;code&gt;OFFSET&lt;/code&gt; keyword.&lt;/li&gt;&lt;li&gt;Can't modify the vault or perform transactions.&lt;/li&gt;&lt;li&gt;Can't return tokens.&lt;/li&gt;&lt;li&gt;Can't return file download or render URLs.&lt;/li&gt;&lt;li&gt;Doesn't support the &lt;code&gt;WHERE&lt;/code&gt; keyword with columns using transient tokenization.&lt;/li&gt;&lt;li&gt;Doesn't support <code>?</code> conditional for columns with column-level encryption disabled.&lt;/li&gt;&lt;ul&gt;
     */
    public CompletableFuture<ApiClientHttpResponse<V1GetQueryResponse>> queryServiceExecuteQuery(
            String vaultId, QueryServiceExecuteQueryBody request) {
        return queryServiceExecuteQuery(vaultId, request, null);
    }

    /**
     * Returns records for a valid SQL query. This endpoint &lt;ul&gt;&lt;li&gt;Can return redacted record values.&lt;/li&gt;&lt;li&gt;Supports only the &lt;code&gt;SELECT&lt;/code&gt; command.&lt;/li&gt;&lt;li&gt;Returns a maximum of 25 records. To return additional records, perform another query using the &lt;code&gt;OFFSET&lt;/code&gt; keyword.&lt;/li&gt;&lt;li&gt;Can't modify the vault or perform transactions.&lt;/li&gt;&lt;li&gt;Can't return tokens.&lt;/li&gt;&lt;li&gt;Can't return file download or render URLs.&lt;/li&gt;&lt;li&gt;Doesn't support the &lt;code&gt;WHERE&lt;/code&gt; keyword with columns using transient tokenization.&lt;/li&gt;&lt;li&gt;Doesn't support <code>?</code> conditional for columns with column-level encryption disabled.&lt;/li&gt;&lt;ul&gt;
     */
    public CompletableFuture<ApiClientHttpResponse<V1GetQueryResponse>> queryServiceExecuteQuery(
            String vaultId, QueryServiceExecuteQueryBody request, RequestOptions requestOptions) {
        HttpUrl httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v1/vaults")
                .addPathSegment(vaultId)
                .addPathSegments("query")
                .build();
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new ApiClientException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl)
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<ApiClientHttpResponse<V1GetQueryResponse>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                    if (response.isSuccessful()) {
                        future.complete(new ApiClientHttpResponse<>(
                                ObjectMappers.JSON_MAPPER.readValue(responseBody.string(), V1GetQueryResponse.class),
                                response));
                        return;
                    }
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    try {
                        if (response.code() == 404) {
                            future.completeExceptionally(new NotFoundError(
                                    ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                            return;
                        }
                    } catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                    }
                    future.completeExceptionally(new ApiClientApiException(
                            "Error with status code " + response.code(),
                            response.code(),
                            ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                            response));
                    return;
                } catch (IOException e) {
                    future.completeExceptionally(new ApiClientException("Network error executing HTTP request", e));
                }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new ApiClientException("Network error executing HTTP request", e));
            }
        });
        return future;
    }
}
